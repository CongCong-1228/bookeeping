import styled from "styled-components";

const Button = styled.button`
  border: none;
  color: red;
  padding: 8px;

  &:hover {
    background: green;
    color: #fff;
    cursor: pointer;
  }
`

export default Button;
